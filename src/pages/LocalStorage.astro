---
const user = {
  id: 1,
  dark_theme: true,
  name: "Aya",
};
---

// @ts-nocheck
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro</title>
  </head>
  <body>
    <h1 id="counter">0</h1>
    <button id="add">+1</button>
    <button id="sub">-1</button>


    <!--?? {
        counter: 0, 
    }; defult value voor als die niet meer iets vindt  -->

    <script>
      // @ts-nocheck
      const saveData = JSON.parse(localStorage.getItem("count")) ?? {
        counter: 0,
      };

      console.log(saveData);
      let value = saveData.counter;
      const counter = document.querySelector("#counter");
      const button = document.querySelector("#add");
      const sub = document.querySelector("#sub");

      counter.textContent = value;


      sub.addEventListener("click", () => {
        value = value - 1;
        counter.textContent = value;
        //een object maken//
        const data = {
          counter: value,
        };
          localStorage.setItem("count", JSON.stringify(data));
      });

      // button.addEventListener("click", function () {});
      add.addEventListener("click", () => {
        value = value + 1;
        counter.textContent = value;
        //een object maken//
        const data = {
          counter: value,
        };
                  //de counter opslaan//
          localStorage.setItem("count", JSON.stringify(data));
        });
  
    </script>
  </body>
</html>


// localStorage.getItem("name")
// localStorage.getItem("ik-besta-nog-niet");
// localStorage.setItem("name", "Aya")

// localStorage.setItem("age", 25)
// localStorage.getItem("age")

// localStorage.setItem("dark_mode", true);
// localStorage.setItem("dark_mode")

// LocalStorage Om data in de browser op te slaan, geen gevoelige data in localstorage opslaan, werkt in de browser niet op de server, locastorage slaat alles op als string er is wel een manier om dat op te lossen 
// JSON.stringify(user)
// LocalStorage.getItem("user")
// const userStr = JSON.stringify(user)
// localStorage.setItem("user", JSON.stringify(user))
// JSON.parse(localStorage.getItem)("user")) 
// tussen aanhalings geven is een key, zonder een variabele   
// localStorage.clear() alles defenititef verwijderen
// localStorage.clear("name")
// Session Storage werkt hetzelfde als localstorage alleen als je de data langer wilt behouden gebruik LocalStorage
